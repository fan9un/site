# 安全与公开发布

本项目不在源码中保存服务凭证。腾讯位置服务、腾讯浏览器地图、天地图、OpenRouteService 与企查查凭证均通过服务端环境变量注入。

## 必须遵守

- 不要提交 `.env.local`、构建产物、日志、截图中的凭证或带凭证的配置文件。
- 复制 `.env.example` 为 `.env.local` 后，仅在本机填写真实值。
- 生产平台应把服务端凭证标记为 Secret，并限制可读取环境变量的人员。
- 腾讯浏览器地图 Key 会在运行时发送到浏览器，这是地图 JavaScript SDK 的工作方式；必须使用“浏览器端”Key，并设置精确域名白名单和配额告警。它不应与服务端 POI Key 共用。
- 腾讯服务端 Key、天地图 Key、OpenRouteService Key 和企查查 Token 不得通过 `NEXT_PUBLIC_*` 暴露。
- 任何曾经发送到聊天、邮件或截图中的真实凭证，在公开发布前都应轮换。

## 公开包

使用 `git archive` 生成公开源码包时，`.gitattributes` 会排除 Sites 项目标识与本地配置。公开包不包含 Git 历史，因此也不会携带提交者邮箱等元数据。

## 泄露处理

发现凭证进入公开仓库后，应先在服务商控制台撤销或轮换凭证，再清理公开仓库和历史；仅删除当前文件不足以使旧凭证失效。
